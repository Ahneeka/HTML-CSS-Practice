// variables
let name = "ahneeka";
console.log(name);

// rules for naming variables
// 1.they can not be a reserved keyword in the sence of put this examples mentioned in the identity/variable place(name ) eg let is one of them and if,else,var,typeof etc
// 2.they should be maningful eg the variable naming should be meaningful not putting a1,x,y,z as variable names 
// variable naming cannot start with a number (e.g.1name)
// variable names cannot contain a space or hyphen (-)

// example of number 8 rule
let firstName = 'Ahneeka';
let lastName = "Jas";

console.log(firstName,lastName);